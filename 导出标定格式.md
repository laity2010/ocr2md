# 按标定导出.

## 导出目录
"chapters/章节目录/output"

## 导出规则如下

### 标题
章节标题模块提供默认勾选的「为标题编号」开关。勾选时，章节内所有已标定为 `1–6 级标题` 的标题，按它们在章节文件中的出现顺序，共享一套三位全局序号；序号不按标题层级分别计数。取消勾选时导出不加序号。数据表预览与该开关保持所见即所得；编号只影响导出，不修改章节工作稿。

例如：
```md
# (001) 一级标题
<br>

## (002) 二级标题
<br>

### (003) 三级标题
<br>

```

### 非法断行
章节工作稿本身不因标定而改写。导出时应用 `非法断行` 模块的人工决策：

- `合并`：删除该候选覆盖的物理换行以及中间空行，把前后正文合为同一行；普通正文之间补一个空格。
- 若前一行以拆词连字符结尾，例如 `exam-` + `ple`，导出为 `example`，不补空格。
- 连续多个 `合并` 断点会作为一个连续正文段合并。
- `已忽略`：保持工作稿中的原始换行，不做合并。
- 该规则同时用于「按标定导出」与「导出标定到trans」。

例如：
```md
原工作稿：
A sentence was broken in the

middle by OCR.

标定为 合并 后导出：
A sentence was broken in the middle by OCR.
<br>
```

### 文本块
```
文本块
<br>

```

### 注释及引用
注释引用导出格式为:
```
[^注释号]
```

注释正文导出格式:
位于章节末尾处.
```
[^注释号]: 注释正文...
<br>

```

### 内嵌块:
嵌入块内部各已标定行严格按其在章节工作稿中的源码行号顺序导出，不再按“标题、图片、HTML、文本”等行类型重新排序。`嵌入块首` 负责块起始，`<embed id=...>` 固定置于该嵌入块内容末尾。

#### 内嵌简单图片
```
>
内嵌标题
内嵌图片链接: ![[下载到本地目录图片的obsidian链接]]
><embed id=01></embed>
<br>

```

#### 内嵌HTML表格
```
>
内嵌标题 (内嵌块的标题文本)
><table><tr><td></td><td>Revenues (in € mil)</td><td>Operating Income (in € mil)</td><td>CAGR in Revenues, 2016–21</td><td>Operating Margin, 2021</td></tr><tr><td>Beauty &amp; Personal Care</td><td>€21,901</td><td>€4,742</td><td>1.66%</td><td>21.65%</td></tr><tr><td></td><td></td><td></td><td></td><td></td></tr><tr><td>Home Care</td><td>€10,572</td><td>€1,417</td><td>1.10%</td><td>13.40%</td></tr><tr><td>Food &amp; Refreshment</td><td>€19,971</td><td>€3,477</td><td>-2.38%</td><td>17.41%</td></tr></table> (表格的HTML源代码)
><embed id=02></embed> (内嵌块的序号id)
<br>

```

#### 内嵌图像HTML表格
```
>
内嵌标题 (内嵌块的标题文本)
![[下载到本地目录图片的obsidian链接]] (内嵌表格图片链接)
>>[! ]- HTML (obsidian callout 格式)
>><table><tr><td></td><td>Revenues (in € mil)</td><td>Operating Income (in € mil)</td><td>CAGR in Revenues, 2016–21</td><td>Operating Margin, 2021</td></tr><tr><td>Beauty &amp; Personal Care</td><td>€21,901</td><td>€4,742</td><td>1.66%</td><td>21.65%</td></tr><tr><td></td><td></td><td></td><td></td><td></td></tr><tr><td>Home Care</td><td>€10,572</td><td>€1,417</td><td>1.10%</td><td>13.40%</td></tr><tr><td>Food &amp; Refreshment</td><td>€19,971</td><td>€3,477</td><td>-2.38%</td><td>17.41%</td></tr></table> (表格的HTML源代码)
><embed id=02></embed> (内嵌块的序号id)
<br>

```